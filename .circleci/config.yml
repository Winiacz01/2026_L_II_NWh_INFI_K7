version: 2.1
jobs:
  build-and-push:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7
          docker_layer_caching: true
      - run:
          name: Push Docker image
          command: make docker_push 
workflows:
  basic:
    jobs:
      - build-and-test
